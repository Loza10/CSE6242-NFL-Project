# Visualiation libraries
streamlit>=1.5.0
pandas>=2.0.0
numpy>=1.24.0
plotly>=6.0.0
matplotlib>=3.7.0
seaborn>=0.13.0

#Model deployment libraries. Comment these out if installation is taking too and you only want to see the dashboard.
torch
tqdm
wheel
